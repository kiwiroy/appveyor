use alienfile;

probe sub { 'share'; };

share {
  plugin 'Fetch::Local' => (
    url => 'dist-1.0.0.tar.gz',
  );

  plugin Extract => 'tar.gz';


  build [
    'sh config -v -t --prefix=/tmp',
    'env PERL=perl sh config -v -t --prefix=/tmp',
    '%{make}',

  ];
};
